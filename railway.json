{
  "deploy": {
    "startCommand": "gunicorn -b 0.0.0.0:$PORT --timeout 120 --workers 2 --preload --access-logfile - --error-logfile - --forwarded-allow-ips * app:app",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
